{
  "version": 2,
  "builds": [
    { "src": "app/**/*", "use": "@vercel/next" },
    { "src": "backend/index.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/backend/index.ts" },
    { "src": "/(.*)", "dest": "/app/$1" }
  ]
}
